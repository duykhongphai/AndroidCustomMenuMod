#pragma once

#include <string>

namespace onyx::demo {

/** Runs a pointer/offset exercise over memory owned by this process. */
[[nodiscard]] std::string runOwnedOffsetLab();

}  // namespace onyx::demo
